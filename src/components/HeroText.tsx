// HeroText.tsx
import React from "react";

const HeroText: React.FC = () => {
    return (
        <h2 className="text-2xl font-medium text-body my-4">
            Upload a plant image for identification or health assessment
        </h2>
    );
};

export default HeroText;
